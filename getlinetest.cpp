#include <iostream>
#include <string>


using namespace std;


void split(string& s, char delim, int index) {
    string ss;
    int iter = 0;
    for (int i = 0; i < s.length(); i++)
    {
        if(s[i] == delim)
        {

        }
    }
}
int main() {
    string teststring = "This is a test";

    split(teststring, ' ', 0);
}
